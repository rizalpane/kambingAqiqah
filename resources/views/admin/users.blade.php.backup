@extends('layouts.layoutAdmin')
@section('title', 'Manajemen Pengguna')
@section('content')

<style>
/* === PAGE STYLES === */
.users-page {
    background: #f8f9fa;
    min-height: 100vh;
    padding: 2rem 0;
}

/* === PAGE HEADER === */
.page-header {
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    padding: 2.5rem 2rem;
    border-radius: 25px;
    margin-bottom: 2rem;
    box-shadow: 0 15px 40px rgba(102, 126, 234, 0.3);
    position: relative;
    overflow: hidden;
}

.page-header::before {
    content: '';
    position: absolute;
    top: -50%;
    right: -10%;
    width: 300px;
    height: 300px;
    background: rgba(255, 255, 255, 0.1);
    border-radius: 50%;
    animation: float 6s ease-in-out infinite;
}

.page-header::after {
    content: '';
    position: absolute;
    bottom: -30%;
    left: -5%;
    width: 250px;
    height: 250px;
    background: rgba(255, 255, 255, 0.08);
    border-radius: 50%;
    animation: float 8s ease-in-out infinite;
    animation-delay: 2s;
}

.page-header-content {
    position: relative;
    z-index: 1;
    display: flex;
    justify-content: space-between;
    align-items: center;
    flex-wrap: wrap;
    gap: 1rem;
}

.page-title {
    color: white;
    font-weight: 900;
    font-size: 2.5rem;
    margin-bottom: 0.5rem;
    text-shadow: 0 4px 15px rgba(0, 0, 0, 0.2);
    display: flex;
    align-items: center;
    gap: 1rem;
}

.page-subtitle {
    color: rgba(255, 255, 255, 0.95);
    font-size: 1.1rem;
    font-weight: 600;
    margin: 0;
}

.header-badge {
    background: rgba(255, 255, 255, 0.2);
    backdrop-filter: blur(10px);
    padding: 0.75rem 1.5rem;
    border-radius: 50px;
    color: white;
    font-weight: 600;
    font-size: 0.95rem;
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    border: 2px solid rgba(255, 255, 255, 0.3);
}

/* === STATS CARDS === */
.stats-card {
    background: white;
    border-radius: 20px;
    padding: 1.5rem;
    display: flex;
    align-items: center;
    gap: 1.5rem;
    box-shadow: 0 5px 20px rgba(0, 0, 0, 0.08);
    transition: all 0.3s ease;
    border: 2px solid transparent;
}

.stats-card:hover {
    transform: translateY(-5px);
    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.12);
}

.stats-card-primary { border-color: #667eea; }
.stats-card-primary .stats-icon { background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); }
.stats-card-success { border-color: #10b981; }
.stats-card-success .stats-icon { background: linear-gradient(135deg, #10b981 0%, #059669 100%); }
.stats-card-info { border-color: #3b82f6; }
.stats-card-info .stats-icon { background: linear-gradient(135deg, #3b82f6 0%, #2563eb 100%); }
.stats-card-warning { border-color: #f59e0b; }
.stats-card-warning .stats-icon { background: linear-gradient(135deg, #f59e0b 0%, #d97706 100%); }

.stats-icon {
    width: 60px;
    height: 60px;
    border-radius: 15px;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 1.8rem;
    color: white;
    flex-shrink: 0;
}

.stats-value {
    font-size: 2rem;
    font-weight: 800;
    color: #1f2937;
    line-height: 1;
}

.stats-label {
    font-size: 0.9rem;
    color: #6b7280;
    font-weight: 600;
}

/* === ALERT STYLES === */
.alert-gradient {
    padding: 1rem 1.5rem;
    border-radius: 15px;
    margin-bottom: 1.5rem;
    border: none;
    animation: slideDown 0.3s ease;
}

.alert-gradient-success {
    background: linear-gradient(135deg, #d1fae5 0%, #a7f3d0 100%);
    color: #065f46;
}

.alert-gradient-danger {
    background: linear-gradient(135deg, #fee2e2 0%, #fecaca 100%);
    color: #991b1b;
}

@keyframes slideDown {
    from { opacity: 0; transform: translateY(-20px); }
    to { opacity: 1; transform: translateY(0); }
}

/* === CUSTOM CARD === */
.custom-card {
    background: white;
    border-radius: 25px;
    box-shadow: 0 5px 25px rgba(0, 0, 0, 0.08);
    overflow: hidden;
    animation: fadeInUp 0.5s ease;
}

@keyframes fadeInUp {
    from { opacity: 0; transform: translateY(30px); }
    to { opacity: 1; transform: translateY(0); }
}

.custom-card-header {
    background: linear-gradient(135deg, #f9fafb 0%, #f3f4f6 100%);
    padding: 1.5rem 2rem;
    border-bottom: 2px solid #e5e7eb;
}

.custom-card-header h5 {
    color: #1f2937;
    font-weight: 700;
    margin: 0;
}

.search-box {
    position: relative;
    width: 300px;
}

.search-box i {
    position: absolute;
    left: 1rem;
    top: 50%;
    transform: translateY(-50%);
    color: #9ca3af;
}

.search-box .form-control {
    padding-left: 2.5rem;
    border-radius: 50px;
    border: 2px solid #e5e7eb;
    transition: all 0.3s ease;
}

.search-box .form-control:focus {
    border-color: #667eea;
    box-shadow: 0 0 0 3px rgba(102, 126, 234, 0.1);
}

.custom-card-body {
    padding: 0;
}

/* === CUSTOM TABLE === */
.custom-table {
    width: 100%;
    margin: 0;
}

.custom-table thead tr {
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
}

.custom-table thead th {
    color: white;
    font-weight: 700;
    text-transform: uppercase;
    font-size: 0.85rem;
    letter-spacing: 0.5px;
    padding: 1.25rem 1.5rem;
    border: none;
    white-space: nowrap;
}

.custom-table tbody tr {
    border-bottom: 1px solid #f3f4f6;
    transition: all 0.3s ease;
}

.custom-table tbody tr:hover {
    background: linear-gradient(135deg, #f9fafb 0%, #f3f4f6 100%);
}

.custom-table tbody td {
    padding: 1.25rem 1.5rem;
    vertical-align: middle;
    color: #4b5563;
}

/* === BADGE ID === */
.badge-id {
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    color: white;
    padding: 0.4rem 1rem;
    border-radius: 50px;
    font-weight: 700;
    font-size: 0.85rem;
    display: inline-block;
}

/* === USER INFO === */
.user-info {
    display: flex;
    align-items: center;
    gap: 1rem;
}

.user-avatar {
    width: 50px;
    height: 50px;
    border-radius: 50%;
    overflow: hidden;
    border: 3px solid #667eea;
    box-shadow: 0 4px 10px rgba(102, 126, 234, 0.3);
}

.user-avatar img {
    width: 100%;
    height: 100%;
    object-fit: cover;
}

.user-name {
    font-weight: 700;
    color: #1f2937;
    font-size: 1rem;
}

.user-role {
    font-size: 0.85rem;
    color: #667eea;
    font-weight: 600;
}

/* === CELL STYLING === */
.email-cell, .phone-cell, .address-cell, .date-cell {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    color: #4b5563;
}

.email-cell i, .phone-cell i, .address-cell i, .date-cell i {
    color: #667eea;
}

/* === ACTION BUTTONS === */
.action-buttons {
    display: flex;
    gap: 0.5rem;
    justify-content: center;
}

.btn-action {
    width: 38px;
    height: 38px;
    border-radius: 10px;
    border: none;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    font-size: 1rem;
    cursor: pointer;
    transition: all 0.3s ease;
}

.btn-action-edit {
    background: linear-gradient(135deg, #3b82f6 0%, #2563eb 100%);
    color: white;
}

.btn-action-edit:hover {
    transform: translateY(-3px);
    box-shadow: 0 8px 15px rgba(59, 130, 246, 0.4);
}

.btn-action-delete {
    background: linear-gradient(135deg, #ef4444 0%, #dc2626 100%);
    color: white;
}

.btn-action-delete:hover {
    transform: translateY(-3px);
    box-shadow: 0 8px 15px rgba(239, 68, 68, 0.4);
}

/* === CUSTOM MODAL === */
.custom-modal {
    border-radius: 25px;
    overflow: hidden;
    border: none;
}

.modal-header-gradient {
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    padding: 1.5rem 2rem;
    border: none;
}

.modal-header-gradient .modal-title {
    color: white;
    font-weight: 700;
    font-size: 1.3rem;
}

.modal-icon-warning {
    width: 80px;
    height: 80px;
    margin: 0 auto;
    background: linear-gradient(135deg, #fef3c7 0%, #fde68a 100%);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 2.5rem;
    color: #f59e0b;
}

.alert-danger-custom {
    background: linear-gradient(135deg, #fee2e2 0%, #fecaca 100%);
    border: none;
    color: #991b1b;
    padding: 1rem;
    border-radius: 15px;
    display: flex;
    align-items: center;
}

.btn-modal {
    padding: 0.75rem 2rem;
    border-radius: 50px;
    border: none;
    font-weight: 700;
    transition: all 0.3s ease;
}

.btn-modal-secondary {
    background: #e5e7eb;
    color: #4b5563;
}

.btn-modal-secondary:hover {
    background: #d1d5db;
    transform: translateY(-2px);
}

.btn-modal-danger {
    background: linear-gradient(135deg, #ef4444 0%, #dc2626 100%);
    color: white;
}

.btn-modal-danger:hover {
    transform: translateY(-2px);
    box-shadow: 0 8px 20px rgba(239, 68, 68, 0.4);
}

/* === EMPTY STATE === */
.empty-state {
    text-align: center;
    padding: 4rem 2rem;
}

.empty-icon {
    width: 120px;
    height: 120px;
    margin: 0 auto 2rem auto;
    background: linear-gradient(135deg, #f3f4f6 0%, #e5e7eb 100%);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 4rem;
    color: #9ca3af;
}

.empty-state h5 {
    color: #1f2937;
    font-weight: 700;
    margin-bottom: 0.5rem;
}

.empty-state p {
    color: #6b7280;
}

/* === ANIMATIONS === */
@keyframes float {
    0%, 100% {
        transform: translateY(0) translateX(0);
    }
    33% {
        transform: translateY(-20px) translateX(10px);
    }
    66% {
        transform: translateY(10px) translateX(-10px);
    }
}

/* === RESPONSIVE === */
@media (max-width: 768px) {
    .page-header-content {
        flex-direction: column;
        text-align: center;
    }
    
    .page-title {
        font-size: 2rem;
        justify-content: center;
    }
    
    .search-box {
        width: 100%;
        margin-top: 1rem;
    }
    
    .custom-card-header {
        flex-direction: column;
        gap: 1rem;
    }
    
    .custom-card-header > div {
        width: 100%;
    }
    
    .stats-card {
        flex-direction: column;
        text-align: center;
    }
    
    .action-buttons {
        flex-wrap: wrap;
    }
}
</style>

<div class="users-page">
    <div class="container-fluid" style="padding: 0 2rem;">
        
        <!-- Page Header -->
        <div class="page-header">
            <div class="page-header-content">
                <div>
                    <h1 class="page-title">
                        <i class="bi bi-people-fill"></i>
                        Manajemen Pengguna
                    </h1>
                    <p class="page-subtitle">Kelola data pengguna sistem</p>
                </div>
                <div class="header-badge">
                    <i class="bi bi-person-circle"></i>
                    <span>{{ $users->count() }} Total Users</span>
                </div>
            </div>
        </div>

        <!-- Stats Cards -->
        <div class="row g-3 mb-4">
            <div class="col-md-3">
                <div class="stats-card stats-card-primary">
                    <div class="stats-icon">
                        <i class="bi bi-people"></i>
                    </div>
                    <div class="stats-content">
                        <div class="stats-value">{{ $users->count() }}</div>
                        <div class="stats-label">Total Users</div>
                    </div>
                </div>
            </div>
            <div class="col-md-3">
                <div class="stats-card stats-card-success">
                    <div class="stats-icon">
                        <i class="bi bi-person-check"></i>
                    </div>
                    <div class="stats-content">
                        <div class="stats-value">{{ $users->where('created_at', '>=', now()->subDays(30))->count() }}</div>
                        <div class="stats-label">User Baru (30 Hari)</div>
                    </div>
                </div>
            </div>
            <div class="col-md-3">
                <div class="stats-card stats-card-info">
                    <div class="stats-icon">
                        <i class="bi bi-envelope-check"></i>
                    </div>
                    <div class="stats-content">
                        <div class="stats-value">{{ $users->whereNotNull('email')->count() }}</div>
                        <div class="stats-label">Email Terverifikasi</div>
                    </div>
                </div>
            </div>
            <div class="col-md-3">
                <div class="stats-card stats-card-warning">
                    <div class="stats-icon">
                        <i class="bi bi-clock-history"></i>
                    </div>
                    <div class="stats-content">
                        <div class="stats-value">{{ $users->where('created_at', '>=', now()->subWeek())->count() }}</div>
                        <div class="stats-label">User Minggu Ini</div>
                    </div>
                </div>
            </div>
        </div>

        @if(session('success'))
            <div class="alert-gradient alert-gradient-success">
                <i class="bi bi-check-circle-fill me-2"></i>
                {{ session('success') }}
            </div>
        @endif

        @if(session('error'))
            <div class="alert-gradient alert-gradient-danger">
                <i class="bi bi-exclamation-triangle-fill me-2"></i>
                {{ session('error') }}
            </div>
        @endif

        <!-- Users Table Card -->
        <div class="custom-card">
            <div class="custom-card-header">
                <div class="d-flex justify-content-between align-items-center flex-wrap">
                    <div>
                        <h5 class="mb-0"><i class="bi bi-table me-2"></i>Daftar Pengguna</h5>
                    </div>
                    <div class="search-box">
                        <i class="bi bi-search"></i>
                        <input type="text" id="searchInput" class="form-control" placeholder="Cari user...">
                    </div>
                </div>
            </div>
            <div class="custom-card-body">
                @if($users->count() > 0)
                    <div class="table-responsive">
                        <table class="table custom-table">
                            <thead>
                                <tr>
                                    <th>ID</th>
                                    <th>Pengguna</th>
                                    <th>Email</th>
                                    <th>Telepon</th>
                                    <th>Alamat</th>
                                    <th>Terdaftar</th>
                                    <th class="text-center">Aksi</th>
                                </tr>
                            </thead>
                            <tbody id="userTableBody">
                                @foreach($users as $user)
                                    <tr class="user-row">
                                        <td>
                                            <div class="badge-id">#{{ $user->id }}</div>
                                        </td>
                                        <td>
                                            <div class="user-info">
                                                <div class="user-avatar">
                                                    @if($user->avatar)
                                                        <img src="{{ asset('storage/' . $user->avatar) }}" alt="Avatar">
                                                    @else
                                                        <img src="{{ asset('images/default-avatar.png') }}" alt="Avatar">
                                                    @endif
                                                </div>
                                                <div class="user-details">
                                                    <div class="user-name">{{ $user->name }}</div>
                                                    <div class="user-role">
                                                        <i class="bi bi-person-badge"></i> User
                                                    </div>
                                                </div>
                                            </div>
                                        </td>
                                        <td>
                                            <div class="email-cell">
                                                <i class="bi bi-envelope"></i>
                                                {{ $user->email }}
                                            </div>
                                        </td>
                                        <td>
                                            <div class="phone-cell">
                                                <i class="bi bi-telephone"></i>
                                                {{ $user->phone ?? '-' }}
                                            </div>
                                        </td>
                                        <td>
                                            <div class="address-cell">
                                                <i class="bi bi-geo-alt"></i>
                                                {{ Str::limit($user->address ?? '-', 30) }}
                                            </div>
                                        </td>
                                        <td>
                                            <div class="date-cell">
                                                <i class="bi bi-calendar-check"></i>
                                                {{ $user->created_at->format('d M Y') }}
                                            </div>
                                        </td>
                                        <td class="text-center">
                                            <div class="action-buttons">
                                                <a href="{{ route('admin.users.edit', $user->id) }}" class="btn-action btn-action-edit" title="Edit User">
                                                    <i class="bi bi-pencil-square"></i>
                                                </a>
                                                <button type="button" class="btn-action btn-action-delete" data-bs-toggle="modal" data-bs-target="#deleteModal{{ $user->id }}" title="Hapus User">
                                                    <i class="bi bi-trash"></i>
                                                </button>
                                            </div>
                                        </td>
                                    </tr>

                                    <!-- Delete Modal -->
                                <div class="modal fade" id="deleteModal{{ $user->id }}" tabindex="-1" aria-hidden="true">
                                    <div class="modal-dialog modal-dialog-centered">
                                        <div class="modal-content custom-modal">
                                            <div class="modal-header-gradient">
                                                <h5 class="modal-title">
                                                    <i class="bi bi-exclamation-triangle me-2"></i>
                                                    Konfirmasi Hapus
                                                </h5>
                                                <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Close"></button>
                                            </div>
                                            <div class="modal-body">
                                                <div class="text-center mb-3">
                                                    <div class="modal-icon-warning">
                                                        <i class="bi bi-person-x"></i>
                                                    </div>
                                                </div>
                                                <p class="text-center mb-2">Apakah Anda yakin ingin menghapus user:</p>
                                                <h5 class="text-center mb-3"><strong>{{ $user->name }}</strong></h5>
                                                <div class="alert alert-danger-custom">
                                                    <i class="bi bi-info-circle me-2"></i>
                                                    Data yang dihapus tidak dapat dikembalikan!
                                                </div>
                                            </div>
                                            <div class="modal-footer">
                                                <button type="button" class="btn-modal btn-modal-secondary" data-bs-dismiss="modal">
                                                    <i class="bi bi-x-circle me-1"></i> Batal
                                                </button>
                                                <form action="{{ route('admin.users.destroy', $user->id) }}" method="POST" class="d-inline">
                                                    @csrf
                                                    @method('DELETE')
                                                    <button type="submit" class="btn-modal btn-modal-danger">
                                                        <i class="bi bi-trash me-1"></i> Hapus
                                                    </button>
                                                </form>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            @endforeach
                        </tbody>
                    </table>
                </div>
            @else
                <div class="empty-state">
                    <div class="empty-icon">
                        <i class="bi bi-people"></i>
                    </div>
                    <h5>Belum Ada Pengguna</h5>
                    <p>Tidak ada pengguna yang terdaftar di sistem.</p>
                </div>
            @endif
        </div>
    </div>
</div>

<script>
// Search functionality
document.getElementById('searchInput').addEventListener('keyup', function() {
    const searchValue = this.value.toLowerCase();
    const tableRows = document.querySelectorAll('#userTableBody tr.user-row');
    
    tableRows.forEach(row => {
        const text = row.textContent.toLowerCase();
        if (text.includes(searchValue)) {
            row.style.display = '';
        } else {
            row.style.display = 'none';
        }
    });
});
</script>

@endsection
